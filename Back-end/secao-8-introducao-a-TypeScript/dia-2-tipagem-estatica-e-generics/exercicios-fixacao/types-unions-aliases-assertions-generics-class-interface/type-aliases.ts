type Bird = {
  color: string,
  species: string,
  isInExtinction: boolean,
  population: number,
}

type address = {
  address: string,
  district: string,
  city: string,
  state: string,
}

type sum = (a:number, b:number) => number;